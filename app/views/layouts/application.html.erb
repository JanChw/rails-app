<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title)%>|RailsApp</title>
    <%= render 'layouts/default'%>
  </head>

  <body>
   <div class='container'>
     <%= render 'layouts/header'%>
    <div class="container">
      <%= yield %>
    </div>
    <%= render 'layouts/footer'%>
   </div>
  </body>
</html>
